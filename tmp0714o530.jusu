function hot():
    x = 0
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    x = x + math.pi
    return x
end
say hot()