# Exercises — Lesson 01

1) Interactive greeting
- Modify `examples/hello.jusu` to accept input and print `Hello, <name>!`.

2) Iterative factorial
- Write `factorial.jusu` in `examples/` implementing iterative factorial for positive integers.

3) Submit
- Create a folder `submissions/lesson-01/<yourname>/` with `.jusu` files and a short README.
- Run `python tools/run_tests.py` to ensure no regressions.
