# Submissions

Place submissions in `course/submissions/<lesson_slug>/<student_name>/`.
Each submission should include a `run.py` or `run.sh` that executes the solution and prints `PASS` on success.
The grader `course/tools/grade_submission.py` runs the script and reports the result in JSON.
