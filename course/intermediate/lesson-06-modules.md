# Lesson 06 — Modules & Packaging with `arpah` 📦

## Objectives
- Create a module and publish as an `arpah` package
- Use `arpah` to install and import packages
- Understand entry points and plugin APIs

## Example: simple plugin
- See `examples/example_plugin/`
- `python tools/init_jusu_package.py example_plugin` scaffolds a package

## Exercises
- Create a small `math_ext` plugin exposing `fib(n)`.
- Publish to local test index (or local path) and install via `arpah`.

Solutions in `course/intermediate/solutions/lesson-06.md`.
